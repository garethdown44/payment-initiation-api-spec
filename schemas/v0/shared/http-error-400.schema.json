{
  "title": "HTTP 500 Error",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": [ "500" ]
    },
    "title": {
      "type": "string",
      "enum": [ "InternalServerError" ]
    },
    "description": {
      "type": "string",
      "enum": [ "The payment infrastructure is busy or unavailable." ]
    }
  },
  "required": [
    "status",
    "title",
    "description"
  ],
  "additionalProperties": false
}
