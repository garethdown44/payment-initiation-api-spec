{
  "title": "Simple Payment Submit Put 400 Response",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": [ "400" ]
    },
    "title": {
      "type": "string",
      "enum": [ "MalformedRequest" ]
    },
    "description": {
      "type": "string",
      "enum": [ "The request has failed business or structural validation. Do not resubmit the request." ]
    }
  },
  "required": [
    "status",
    "title",
    "description"
  ],
  "additionalProperties": false
}
