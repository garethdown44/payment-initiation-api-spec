{
  "title": "Simple Payment Submit Put 504 Response",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": [ "504" ]
    },
    "title": {
      "type": "string",
      "enum": [
        "Timeout"
      ]
    },
    "description": {
      "type": "string",
      "enum": [
        "Payment request timed out."
      ]
    }
  },
  "required": [
    "status",
    "title",
    "description"
  ],
  "additionalProperties": false
}
