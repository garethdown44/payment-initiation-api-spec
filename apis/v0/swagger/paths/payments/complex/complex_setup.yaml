post:
  parameters:
   - name: body
     in: body
     description: Payload needed to setup a complex payment
     required: true
     schema:
       $ref: ../../../../../../compiled/schemas/v0/payments/complex_setup.post.request.schema.yaml
  responses:
    200:
      description: Payment setup successfully
      schema:
        $ref: ../../../../../../compiled/schemas/v0/payments/complex_setup.post.200.response.schema.yaml
    400:
      description: Bad Request
      schema:
        $ref: ../../../../../../compiled/schemas/v0/shared/http-error-400.schema.yaml
    401:
      description: Unauthorized
      schema:
        $ref: ../../../../../../compiled/schemas/v0/shared/http-error-401.schema.yaml
    500:
      description: Internal Server Error
      schema:
        $ref: ../../../../../../compiled/schemas/v0/shared/http-error-500.schema.yaml
